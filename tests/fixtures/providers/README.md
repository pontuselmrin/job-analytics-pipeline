# Provider Fixtures

Place sanitized HTML/PDF/API fixtures by provider here (e.g. `workday/`, `taleo/`, `smartrecruiters/`).
These fixtures are used by fast unit tests that should not depend on live network behavior.
